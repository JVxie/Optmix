# OptiMix - 最优配比计算器

![icon](./public/icons/png/256x256.png)

<p align="center">
<strong>基于线性规划的原料配比优化工具</strong>
</p>


<p align="center">
<a href="#功能特性">功能特性</a> •
<a href="#快速开始">快速开始</a> •
<a href="#使用指南">使用指南</a> •
<a href="#技术架构">技术架构</a> •
<a href="#构建部署">构建部署</a>
</p>

---

## 📖 项目简介

OptiMix 是一款跨平台的原料配比优化计算器，利用**线性规划算法**帮助用户在满足多种指标约束条件下，自动计算出**成本最低**的原料混合配方。

适用于饲料加工、食品生产、化工配料等需要多原料混合且受指标约束的行业场景。

（PS：几乎由 Gemini 3 及 Claude 4.5 生成）

## ✨ 功能特性

### 🎯 核心功能

- **智能优化计算** - 基于线性规划求解器，自动寻找满足所有约束条件的最优解
- **多指标约束** - 支持设置多个质量指标（如水分、蛋白质等）的最小/最大值范围
- **实时成本估算** - 根据配比方案实时计算混合物单价
- **手动配比调整** - 支持手动滑块调整配比，实时校验指标合规性

### 📊 数据管理

- **多方案管理** - 支持创建、切换、重命名、删除多个配方方案
- **批量操作** - 支持批量删除指标、货物和方案
- **Excel 导入/导出** - 完整的 Excel 数据交换能力，便于与外部系统协作
- **本地持久化** - 所有数据自动保存到本地存储

### 🎨 用户体验

- **深色/浅色主题** - 自适应系统主题，支持手动切换
- **响应式设计** - 完美适配桌面端、平板和手机
- **流畅动画** - 精心设计的过渡动画，提升操作体验
- **安全区域适配** - 完美支持 iOS 刘海屏和 Android 状态栏

### 🚀 跨平台支持

| 平台 | 状态 | 说明 |
|------|------|------|
| 🌐 Web | ✅ | 现代浏览器直接访问 |
| 🤖 Android | ✅ | 原生 APK，Capacitor 构建 |
| 🍎 iOS | 🔧 | 原生 App，Capacitor 构建（需配置） |
| 🖥️ Windows | ✅ | Electron 桌面应用 |
| 🐧 Linux | 🔧 | Electron 支持（需配置） |
| 🍏 macOS | 🔧 | Electron 支持（需配置） |

## 📷 界面预览

### 配置视图
管理指标和货物数据，支持批量操作。

### 计算视图
左侧智能计算最优解，右侧手动调整配比并实时校验。

## 🚀 快速开始

### 环境要求

- Node.js 18+
- npm 或 yarn
- (Android) Android Studio + JDK 17
- (iOS) Xcode 15+ (macOS)
- (Desktop) Electron 构建环境

### 安装依赖

```bash
# 克隆项目
git clone https://github.com/yourusername/optimix.git
cd optimix

# 安装依赖
npm install
```

### 开发模式

```bash
# Web 开发服务器
npm run dev

# Electron 开发模式
npm run electron:dev
```

### 构建发布

```bash
# Web 生产构建
npm run build

# Android 构建
npm run android:build

# Windows 安装包
npm run electron:build:win
```

## 📚 使用指南

### 1. 创建方案

点击侧边栏的「新建方案」按钮，创建一个新的配方方案。

### 2. 配置指标

在「配置」视图的左侧「指标管理」区域：
- 点击「新增」添加质量指标（如水分、蛋白质）
- 设置每个指标的单位、最小值和最大值

### 3. 添加货物

在「配置」视图的右侧「货物管理」区域：
- 点击「新增」添加原料货物
- 填写货物名称、单价（元/吨）
- 填写该货物对应每个指标的数值

### 4. 计算最优配比

切换到「计算」视图：
- 点击「计算最优解」自动寻找成本最低的配比
- 或使用右侧滑块手动调整配比，实时查看指标校验结果

### 5. 导入/导出数据

在侧边栏点击「导入/导出」按钮：
- **导出**：将当前方案保存为 Excel 文件
- **导入**：从 Excel 文件导入方案数据

> ⚠️ 建议先导出一份模板，在模板基础上编辑数据后再导入。

## 🏗️ 技术架构

### 前端框架

```
React 19 + TypeScript + Vite
├── 状态管理: React Hooks (useState, useEffect, useMemo)
├── 样式方案: Tailwind CSS + 深色模式支持
├── 图标库: Lucide React
├── 图表库: Recharts
└── 动画: CSS Transitions + Tailwind animate
```

### 核心算法

```
javascript-lp-solver
├── 目标函数: 最小化总成本
├── 约束条件: 
│   ├── 总配比 = 100%
│   └── 各指标在 [min, max] 范围内
└── 输出: 各原料的最优占比
```

### 跨平台方案

```
├── Web: Vite 直接构建
├── Mobile: Capacitor 8
│   ├── @capacitor/filesystem - 文件操作
│   ├── @capacitor/share - 系统分享
│   ├── @capacitor/status-bar - 状态栏控制
│   └── capacitor-plugin-safe-area - 安全区域
└── Desktop: Electron 40
```

### 项目结构

```
optimix/
├── components/           # React 组件
│   ├── App.tsx          # 主应用组件
│   ├── Sidebar.tsx      # 侧边栏（方案管理）
│   ├── ConfigurationView.tsx  # 配置视图
│   ├── CalculationView.tsx    # 计算视图
│   ├── IndicatorManager.tsx   # 指标管理
│   ├── MaterialManager.tsx    # 货物管理
│   └── Modal.tsx        # 模态框组件
├── services/            # 服务层
│   ├── solver.ts        # 线性规划求解器
│   └── excelService.ts  # Excel 导入导出
├── types.ts             # TypeScript 类型定义
├── index.tsx            # 应用入口
├── index.css            # 全局样式
├── index.html           # HTML 模板
├── capacitor.config.ts  # Capacitor 配置
├── vite.config.ts       # Vite 配置
├── tailwind.config.js   # Tailwind 配置
└── package.json         # 项目依赖
```

## 🔧 配置说明

### Capacitor 配置 (移动端)

```typescript
// capacitor.config.ts
const config: CapacitorConfig = {
  appId: 'com.jvxie.optimix',
  appName: 'OptiMix',
  webDir: 'dist',
  plugins: {
    StatusBar: {
      overlaysWebView: true,      // WebView 延伸到状态栏
      style: 'LIGHT',             // 状态栏图标颜色
      backgroundColor: '#00000000' // 透明背景
    }
  }
};
```

### Vite 配置

```typescript
// vite.config.ts
export default defineConfig({
  base: './',  // 使用相对路径，兼容移动端
  build: {
    outDir: 'dist',
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'chart-vendor': ['recharts']
        }
      }
    }
  }
});
```

## 📦 构建部署

### Web 部署

```bash
npm run build
# 将 dist/ 目录部署到任意静态托管服务
```

### Android APK

```bash
# 1. 构建并同步
npm run android:sync

# 2. 打开 Android Studio
npm run android:open

# 3. 在 Android Studio 中 Build > Generate Signed Bundle/APK
```

### Windows 安装包

```bash
npm run electron:build:win
# 输出在 release/ 目录
```

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 开源协议

本项目基于 [MIT License](LICENSE) 开源。

## 🙏 致谢

- [javascript-lp-solver](https://github.com/JWally/jsLPSolver) - 线性规划求解器
- [Capacitor](https://capacitorjs.com/) - 跨平台移动开发框架
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架
- [Lucide](https://lucide.dev/) - 精美的开源图标库
- [Claude](https://claude.ai) - Anthropic 开发的 AI 助手
- [Gemini](https://aistudio.google.com/) - 由 Google DeepMind 团队打造的高级 AI 助手

---

<p align="center">
Made with ❤️ by <a href="https://jvxie.com">JVxie</a>
</p>
